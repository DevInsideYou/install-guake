# Installs [Guake](http://guake-project.org/) on [Ubuntu](https://www.ubuntu.com/)

```bash
wget -q https://script.install.devinsideyou.com/guake
sudo chmod +x guake && ./guake
```

The official ppa is not up to date.
